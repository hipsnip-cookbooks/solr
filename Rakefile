require 'rake'
require 'cookbook-development/tasks'

COOKBOOK_NAME = "hipsnip-solr"
PACKAGE_DIR = File.dirname(__FILE__) # where to put the compressed cookbook bundle

task :default => 'cookbook:test'

